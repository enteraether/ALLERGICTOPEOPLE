<%= form_for @recing do |f|%>
<%= f.label :ingredient_id, "Ingredient:" %>
<%= f.collection_select :ingredient_id, @ingredients, :id, :name  %>
<%= f.label :recipe_id, "Recipes:" %>
<%= f.collection_select :recipe_id, @recipes, :id, :name  %>

<%= f.submit %>
<% end %>